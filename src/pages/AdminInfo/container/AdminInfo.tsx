import React from "react";
import withTitle from "../../../hoc/withTitle";
import AppCompose from "../../../utils/AppCompose";

const AdminInfo = () => {
    return <></>;
};

export default AppCompose(withTitle)(AdminInfo);
