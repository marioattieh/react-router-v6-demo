import React from "react";
import withTitle from "../../../../hoc/withTitle";
import AppCompose from "../../../../utils/AppCompose";

const SharedNestedIndex = () => {
    return <></>;
};

export default AppCompose(withTitle)(SharedNestedIndex);
