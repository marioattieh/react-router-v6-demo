import React from "react";
import withTitle from "../../../hoc/withTitle";
import AppCompose from "../../../utils/AppCompose";

const AdminProfile = () => {
    return <></>;
};

export default AppCompose(withTitle)(AdminProfile);
