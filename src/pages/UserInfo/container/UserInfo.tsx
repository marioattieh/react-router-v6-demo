import React from "react";
import withTitle from "../../../hoc/withTitle";
import AppCompose from "../../../utils/AppCompose";

const UserInfo = () => {
    return <></>;
};

export default AppCompose(withTitle)(UserInfo);
