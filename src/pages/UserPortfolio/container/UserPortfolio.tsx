import React from "react";
import withTitle from "../../../hoc/withTitle";
import AppCompose from "../../../utils/AppCompose";

const UserPortfolio = () => {
    return <></>;
};

export default AppCompose(withTitle)(UserPortfolio);
